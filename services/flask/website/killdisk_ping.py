from . import db, hddEngine
from .models import DISKS, BATCHES
from sqlalchemy import desc









# import re
# import subprocess

# class Ping:
#     def __init__(self, destination, ping_time, ttl, bytes):
#         self.destination = destination
#         self.ping_time = ping_time
#         self.ttl = ttl
#         self.bytes = bytes
#
#         def __repr__(self):
#             return f'PING: {self.bytes} bytes to {self.destination} in {self.ping_time} ms, ttl: {self.ttl}'
#

